services:
  icloud-hme-gen:
    build: .
    container_name: icloud-hme-gen
    dns:
      - 8.8.8.8
      - 8.8.4.4
    environment:
      - AMOUNT=${AMOUNT:-10}
      - ALLOW_DOT=${ALLOW_DOT:-y}
    volumes:
      - ./:/app/data
    restart: unless-stopped
